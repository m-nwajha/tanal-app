import { PAGES_META_DATA } from '@/constants/META_DATA';
import BoardDirectorsPage from '@/components/views/BoardDirectorsPage';

export const metadata = {
  title: PAGES_META_DATA.boardDirectors,
};
const BoardDirectors = () => <BoardDirectorsPage />;

export default BoardDirectors;
